# TO DO

General:

- check the contour behaviour (contour, contourf, ...) and export to .pvivx
- add more matplotlib charts to the 'heatmap & contour': tricontour, tricountourf, triplot, tripcolor
- add more matplotlib charts: create a '3D plots' mode and add: 'scatter3d', 'bar3d', 'pie3d', 'boxplot3d'
- update the docs
- update the docs on how to use plotviz with pandas

SNS charts:

- add a button to generate python code for SNS chart.
- add other SNS chart types
- allow merging several SNS chart types if compatible

GIS maps:

- add a form for gis mapping
